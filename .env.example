# ===========================================================================
# DevAdmin Docker Compose Environment Variables (Optional root overrides)
# ===========================================================================

# Host port bindings for localhost reverse proxy (defaults: 8107, 8108)
FRONTEND_PORT=8107
BACKEND_PORT=8108

# Vite API Base URL for frontend build
VITE_API_BASE_URL=https://devadmin-api.logicbyroshan.in
